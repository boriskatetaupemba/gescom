# Database schema

InvoicePlus 1.0.0 creates no database table. The warehouse filter uses the standard `facture`, `facturedet`, `facture_extrafields`, `entrepot`, and `societe_commerciaux` tables with `MAIN_DB_PREFIX`.
